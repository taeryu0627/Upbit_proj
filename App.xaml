<Application
    x:Class="Upbit_proj.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:local="clr-namespace:Upbit_proj" 
    xmlns:vm="clr-namespace:Upbit_proj.ViewModel"
    StartupUri="MainWindow.xaml" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    d1p1:Ignorable="d" 
    xmlns:d1p1="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="clr-namespace:Upbit_proj.View" >
    
  <Application.Resources>
    <ResourceDictionary>
            <vm:ViewModelLocator x:Key="Locator" d:IsDataSource="True"/>
            <DataTemplate DataType="{x:Type vm:MainPageViewModel}">
                <view:MainPageView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SelectedViewModel}">
                <view:SelectedView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:PopupBuyViewModel}">
                <view:P_BuyView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:PopupResultViewModel}">
                <view:P_ResultView />
            </DataTemplate>
        </ResourceDictionary>
  </Application.Resources>
</Application>